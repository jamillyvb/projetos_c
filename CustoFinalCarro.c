#include <stdio.h>

int main(){

    float preco_fab, p_dist, p_imp, z;

    scanf("%f %f %f", &preco_fab, &p_dist, &p_imp);
    
    z = 
    scanfss
    

    printf("O VALOR DO CARRO E = %.2f\n", z);

    return 0;
}