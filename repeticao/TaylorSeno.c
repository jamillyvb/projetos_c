#include <stdio.h>
#include <math.h>

int main(){
    
    int n, x, i=0;
    double sin;

    scanf("%d %d", &x, &n);

    for(){
        sin = ((pow(-1, n))*(pow(x, ((2*n)+1))))/((2*n)+1);
    }
    return 0;
}